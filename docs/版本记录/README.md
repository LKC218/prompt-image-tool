# 版本记录索引

本目录存放版本迭代历史和变更记录。

## 文档清单

| 文件 | 职责 |
|------|------|
| [更新记录规范](./更新记录规范.md) | **格式固定要求**（词表、模板、检查清单）；写更新说明必读 |
| [更新记录](./changelog.md) | 版本变更**唯一权威源** |
| [README.md](./README.md) | 本索引；仅保留最新数条摘要 |

发版流程见 [版本发布与更新记录维护指南](../工程指南/版本发布与更新记录维护指南.md)。

## 最新版本

- `v2.5.29`：修复在线更新下载完成后安装程序未执行。
- `v2.5.28`：更新进度紧凑步进器；任务菜单功能分组；导图拖拽落点更清晰。
- `v2.5.27`：在线更新先卸载再装；识别 app.exe 并统一主程序名。
- `v2.5.26`：修复安装包内开屏像素聚合动画不播放。
- `v2.5.25`：安装包统一为 Tauri 主路径，窗口与自动更新一致。
- `v2.5.24`：自动更新能正确覆盖旧版并在安装后自动重启进入新版本。
- `v2.5.23`：导图短标题横排完整可读，不再被挤成竖排单字。
- `v2.5.22`：安装/卸载前结束残留后台服务，覆盖安装不再被文件占用挡住。
- `v2.5.21`：修复 Tauri 包检查更新连不上本地后端；检查结果提示更明确。
- `v2.5.20`：目标计划拖拽调级（列表/导图）；导图长标题完整显示；更新记录阶段轨跟手；开屏像素聚合动画。
- `v2.5.19`：修复自动更新确认卡死、取消按钮点空与空格安装路径；更新会话互斥与文案优化。
- `v2.5.18`：侧栏列表边缘羽化；安装向导全面中文化（含静默安装不自启）。
- `v2.5.17`：Tauri 无边框壳收敛；修复顶栏黑边与最小化缩窗；Python Sidecar 随包分发。
- `v2.5.16`：思维导图可导入图片（工具栏/节点 + /节点菜单）；列表与导图图片入口统一。
- `v2.5.15`：隐藏原生标题栏并修复顶栏黑边与接缝。
- `v2.5.14`：窗口顶栏真实布局行与双端拖动热区。
- `v2.5.13`：一体化窗口外壳（无边框 + 自定义窗口控件）。
- `v2.5.12`：导图节点图片预览与「在导图中定位」；首页统计卡液面；视图路由与导图高度优化。
- `v2.5.11`：目标计划思维导图全链路（结构导图、完成态、新拟态 UI、交互与相机动画）。
- `v2.5.10`：修复确认弹窗与右键菜单中文乱码；移动端筛选标签不再被压缩；新增资源与编码回归校验。
- `v2.5.9`：应用内更新静默安装后自动重启进入新版本；安装目录覆盖与版本校验加固。
- `v2.5.8`：侧栏更多菜单与卡片 3D 视差；图片查看器 FLIP 转场；目标封面自动压缩 WebP。
- `v2.5.7`：首页挂机种植物（30 天轮回、养护、本地 plant.json 持久化）。
- `v2.5.6`：检查更新增加重试与镜像回退，规避 GitHub SSL 中断；网络失败提示改为可读中文。
- `v2.5.5`：PC 应用内更新增加阶段式进度弹窗（下载/校验/安装三阶段），支持取消与重试；设置页独立「软件更新」卡片。
- `v2.5.4`：侧栏新增「摸鱼时间」游戏中心，含俄罗斯方块与飞机大战。
- `v2.5.3`：PC 端应用内自动更新：启动静默检查、设置页手动检查、下载校验后静默安装。
- `v2.5.2`：PC 自定义光标重构为品牌色圆环柔光，更轻更跟手。
- `v2.5.1`：目标计划项目支持多维排序；封面加载失败回退首字母渐变；清理时保护封面。
- `v2.5.0`：PC 与移动端新增目标计划清单模块（工程列表、父子任务、图片管理）。
