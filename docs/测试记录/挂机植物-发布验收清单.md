# 挂机植物 · 发布验收清单

> 打包/发版前逐项勾选。安装版探针见 `build/probe_install.py`（含 `/api/plant`）。

## 1. 开发机（Vite + Python）

- [ ] `npm test -- src/js/plant-core.test.js src/js/plant-persist.test.js` 全过
- [ ] `npm run build` 成功
- [ ] 清 `%APPDATA%\PromptImageManager\data\plant.json` + 清 LS → 首页第 1 天空盆
- [ ] 无「测」调试按钮
- [ ] 浇水/施肥/除害动画正常；园艺光标正常
- [ ] 第 30 天出现「铲除 · 可重开」；确认后回第 1 天
- [ ] 盆角徽章显示 `N/30`，title 含本轮天数
- [ ] 设置 → 重置挂机植物 可用
- [ ] 停 8888 后养护仍成功，出现「保存暂存本地」

## 2. 安装包探针

```text
python build\probe_install.py
```

- [ ] 全部 `[通过]`，含：
  - GET/POST `/api/plant`
  - `plant.json` 落盘
  - `/api/export` 含 plant

## 3. 干净环境手动

- [ ] 新装启动 → 第 1 天
- [ ] 养护后杀进程重开 → 状态仍在
- [ ] 卸载前备份 `plant.json`，重装后放回 → 进度恢复（若安装程序不删 APPDATA）

## 4. 视觉抽检

| 场景 | 期望 |
|------|------|
| day 1 | 01-seed |
| day 10 | 04-juvenile |
| day 24 | 10-bloom-full |
| day 30 | 12-dead + 铲除钮 |
| 深色模式 | 整体可读 |

## 5. 已知限制

- 双开窗口 last-write-win
- 改系统时间会导致 day 跳变
- ZIP 备份暂不含 plant 字段（JSON 备份已含）
