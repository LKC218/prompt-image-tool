# Tauri安装器壳-全页面功能完善测试记录

## 测试范围

本记录对应 `docs/计划文档/Tauri安装器壳-全页面功能完善计划-260511.md` 的实施结果，覆盖：

- 7 步安装器壳页面流转。
- 安装位置路径编辑、目录选择命令和路径校验。
- 附加任务状态贯穿、桌面快捷方式和开始菜单快捷方式收尾。
- 正在安装页阶段式进度、失败重试和修改设置入口。
- 完成页启动主程序、快捷方式收尾和状态反馈。
- Tauri debug 构建与 NSIS 静默安装核心闭环。

## 命令验证

已通过：

```powershell
node --check installer-shell/src/main.js
Get-ChildItem installer-shell/src/pages -Filter *.js | ForEach-Object { node --check $_.FullName }
cargo check --manifest-path installer-shell/src-tauri/Cargo.toml
npm.cmd --prefix installer-shell run tauri -- build --debug
```

Tauri debug 构建产物：

```text
installer-shell/src-tauri/target/debug/prompt_image_manager_installer_shell.exe
```

## 静态预览

已通过本地 HTTP 服务预览 `installer-shell/src`，并使用 Chrome headless 完成 1280 x 720 静态截图检查。截图证据按仓库清理要求不纳入版本控制。

结论：

- 7 个步骤页面均可渲染。
- 准备安装页按钮与任务摘要不再重叠。
- 完成页新增开始菜单入口后仍保持在书页安全区内。

## NSIS 安装核心闭环

测试安装包：

```text
build/PromptImageManager-Setup-2.3.1.exe
```

测试目录：

```text
%LOCALAPPDATA%\PIM-Test-Codex-260511
```

结果：

- 静默安装退出码：`0`。
- `PromptImageManager.exe`：存在。
- `uninstall.exe`：存在。
- 安装目录文件数：`1071`。
- 主程序启动后保持运行，并生成 `app.log`。
- 日志显示服务启动到 `0.0.0.0:8888`。
- 桌面快捷方式：存在。
- 开始菜单主程序快捷方式：存在。
- 开始菜单卸载快捷方式：存在。
- 静默卸载退出码：`0`。
- 卸载后测试目录、桌面快捷方式和开始菜单目录均已清理。

## 剩余风险

- 已验证 Tauri debug 构建和 NSIS 安装核心闭环；未在本轮完成 Tauri WebView 中真实点击触发 `install_with_nsis` 的自动化录制，后续正式打包前仍建议补一次手工或稳定 UI 自动化点击验证。
- 目录选择窗口由 Rust 侧 PowerShell WinForms 对话框实现，需在真实 Tauri 窗口中补一次手工确认焦点和返回值体验。
