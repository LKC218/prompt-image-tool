# 界面6：设置与本地存储

> 版本：1.0 | 最后更新：2026-05-05

---

## 1. 页面定位与目标

展示应用设置和本地存储信息，强调本地安全：
- 外观设置（深色模式/主题色/跟随系统）
- 本地存储数据查看
- 数据备份与恢复
- 本地安全提示

**核心目标**：让用户安心使用，明确数据安全，提供数据管理入口。

---

## 2. 页面结构

```
┌──────────────────────────────────────────────┤
│  设置                                        │
├──────────────────────────────────────────────┤
│  ┌──────────────────────────────────────────┐│
│  │  🐕 柯基穿绿帽衫挥手                    ││
│  │      ⭐                                  ││
│  │  ┌──────────────────────────────┐        ││
│  │  │ 一切数据仅在本地              │        ││
│  │  │ 安全又放心~                   │        ││
│  │  └──────────────────────────────┘        ││
│  │  浅蓝色渐变横幅                          ││
│  └──────────────────────────────────────────┘│
│                                              │
│  外观设置                                     │
│  ┌──────────────────────────────────────────┐│
│  │ 🌙 深色模式                    [开关]    ││
│  ├──────────────────────────────────────────┤│
│  │ 🎨 应用主题色                            ││
│  │     ●✓ ● ● ● ●                          ││
│  │     粉 绿 蓝 紫 黄                       ││
│  ├──────────────────────────────────────────┤│
│  │ 🖥 跟随系统                    [开关]    ││
│  └──────────────────────────────────────────┘│
│                                              │
│  本地存储                                     │
│  ┌──────────────┐ ┌──────────────────────┐   │
│  │ 本地数据大小  │ │ 存储空间             │   │
│  │   128 MB     │ │ 可用 45.6 GB        │   │
│  │   已使用     │ │ 共 64 GB            │   │
│  │  浅蓝卡片    │ │  🟢 环形图          │   │
│  └──────────────┘ └──────────────────────┘   │
│                                              │
│  数据备份与恢复                               │
│  ┌──────────────┐ ┌──────────────────────┐   │
│  │ 💾 本地备份  │ │ 📥 导入备份          │   │
│  │ 导出所有数据 │ │ 恢复整套数据         │   │
│  │  绿色        │ │  蓝色               │   │
│  ├──────────────┼────────────────────────┤   │
│  │ 📄 导入对话  │                        │   │
│  │ 从单条对话新建│                        │   │
│  │  紫色        │                        │   │
│  └──────────────┘                        │   │
│                                              │
│  ┌──────────────────────────────────────────┐│
│  │ 🔒 本应用完全本地运行，不联网，          ││
│  │    不收集任何数据。                  →   ││
│  │  浅绿色安全提示栏                        ││
│  └──────────────────────────────────────────┘│
│                                              │
│  ─── Home Indicator ───                      │
└──────────────────────────────────────────────┘
```

---

## 3. 视觉规范

### 3.1 页面专用颜色

| 元素 | 颜色 | 说明 |
|------|------|------|
| 柯基安全横幅背景 | 浅蓝色渐变 | `linear-gradient(135deg, #E8F4FF, #D0E8FF)` |
| 对话气泡 | `--m-surface` 白色 | 白色圆角气泡 |
| 设置卡片 | `--m-surface` 白色 | 白色 |
| 开关-关闭 | `--m-border` 灰色 | 灰色轨道 |
| 开关-开启 | `--m-green` 绿色 | 绿色轨道 |
| 主题色圆点-选中 | 对应颜色 + 白色勾 | 带勾选标记 |
| 主题色圆点-未选中 | 对应颜色 | 无勾选 |
| 本地数据卡片 | `--m-blue-light` 浅蓝 | 浅蓝背景 |
| 存储空间卡片 | `--m-surface` 白色 | 白色 |
| 备份按钮-绿色 | `--m-green-light` 背景 + `--m-green` 图标 | 绿色 |
| 导入按钮-蓝色 | `--m-blue-light` 背景 + `--m-blue` 图标 | 蓝色 |
| 导入对话-紫色 | `--m-purple-light` 背景 + `--m-purple` 图标 | 紫色 |
| 安全提示栏 | `--m-green-light` 背景 | 浅绿色 |

### 3.2 尺寸规范

| 属性 | 值 |
|------|-----|
| 柯基横幅圆角 | `--m-radius-lg`（24px） |
| 柯基横幅内边距 | 20px |
| 设置卡片圆角 | `--m-radius-lg`（24px） |
| 设置行高度 | 56px |
| 开关尺寸 | 51×31px |
| 主题色圆点 | 28×28px |
| 备份恢复卡片圆角 | `--m-radius-md`（16px） |
| 环形图尺寸 | 60×60px |

---

## 4. 组件清单

### 4.1 可复用组件

| 组件 | 用途 |
|------|------|
| `mascot` | 柯基设置页插画 |

### 4.2 页面专用组件

| 组件 | 说明 |
|------|------|
| 柯基安全横幅 | 浅蓝渐变+柯基插画+对话气泡 |
| 设置行 | 图标+文字+右侧控件（开关/颜色/箭头） |
| 主题色选择器 | 一排颜色圆点，选中带勾 |
| 存储信息卡片 | 数据大小+环形图 |
| 备份恢复卡片 | 两列网格中的三张彩色操作卡片 |
| 图片下载记录 | 最近下载图片列表 + 数量徽标 + 一键清空历史，长文件名、时间和平台信息采用紧凑抗挤压排版 |
| 局域网同步卡片 | 搜索 PC、同步方向切换、测试连接、手动 IP、设备结果列表、同步前冲突预览、确认策略、同步进度和结果报告 |
| PC 搜索结果项 | 设备名+IP+来源标签+互通能力状态 |
| 安全提示栏 | 浅绿色+锁图标+文字+箭头 |

---

## 5. 数据绑定

| 数据 | 来源 | API/方法 |
|------|------|---------|
| 深色模式状态 | localStorage | `localStorage.getItem('theme')` |
| 主题色 | localStorage | Phase 1 mock |
| 跟随系统 | localStorage | `window.matchMedia('(prefers-color-scheme)')` |
| 本地数据大小 | 文件系统 | Phase 1 mock（128MB） |
| 存储空间 | Capacitor Filesystem API | Phase 1 mock |
| 备份/导入 | 现有 API | `exportData()` / `importData()` |
| 图片下载历史 | localStorage | `download-history.js` |
| 局域网 PC 搜索 | 局域网 HTTP 健康检查 | `LanScanner.scan()` |
| 局域网同步 | 现有 API | `LanSync.preview()`、`LanSync.sync()`、`LanSync.push()`、`LanSync.bidirectional()` |

---

## 6. 交互规范

### 6.1 交互状态矩阵

| 元素 | 默认态 | 触摸态 | 选中态 |
|------|--------|--------|--------|
| 深色模式开关 | 灰色轨道 | — | 绿色轨道+白色滑块 |
| 跟随系统开关 | 灰色轨道 | — | 绿色轨道+白色滑块 |
| 主题色圆点 | 对应颜色 | `scale(1.15)` | 带白色勾选+外阴影 |
| 设置行 | 白色背景 | 背景→`--m-surface2` | — |
| 备份恢复卡片 | 各自浅色背景 | `scale(0.97)` | — |
| 图片下载记录 | 图标+文件名+来源/保存位置+时间平台标签 | 清空历史按钮 `scale(0.97)` | 空记录时清空按钮弱化禁用 |
| 安全提示栏 | 浅绿背景 | 背景→加深 | — |

### 6.2 手势操作

| 手势 | 目标 | 行为 |
|------|------|------|
| 点击 | 深色模式开关 | 切换暗色/亮色主题 |
| 点击 | 主题色圆点 | 选中该主题色 |
| 点击 | 跟随系统开关 | 切换跟随系统偏好 |
| 点击 | 本地备份 | 触发数据导出 |
| 点击 | 导入备份 | 触发文件选择器 |
| 点击 | 导入对话 | 选择单条对话归档 .json 后进入新建提示词页 |
| 点击 | 清空图片下载历史 | 弹出确认后清空本地历史记录 |
| 点击 | 搜索 PC | 搜索同一局域网内已打开的 PC 端 |
| 点击 | PC 搜索结果项 | 自动填入 IP 并测试连接，成功后标记为可同步 |
| 点击 | 测试连接 | 校验输入的 PC IP 是否可用 |
| 点击 | 同步方向 | 在“从 PC 拉取 / 回传到 PC / 双向同步”之间切换 |
| 点击 | 同步 | 先生成冲突预览，再按当前方向执行拉取、回传或双向同步 |
| 点击 | 安全同步 | 冲突项生成副本，无冲突项继续同步 |
| 点击 | 只同步无冲突项 | 跳过同 ID 冲突项，仅同步安全新增项 |
| 点击 | 安全提示栏 | 展开安全说明详情 |

### 6.3 动画过渡

| 动画 | 时长 | 说明 |
|------|------|------|
| 主题切换 | 0.25s | 背景色+文字色渐变 |
| 开关切换 | 0.2s | 滑块滑动 |
| 主题色选中 | 0.15s | 勾选标记缩放弹入 |
| 备份恢复卡片触摸 | 0.1s | `scale(0.97)` |

### 6.4 异常状态

| 状态 | 视觉 | 文案 |
|------|------|------|
| 导出失败 | Toast 错误提示 | 「导出失败，请重试」 |
| 导入失败 | Toast 错误提示 | 「导入失败，文件格式不正确」 |
| 存储空间不足 | 安全提示栏变红 | 「存储空间不足，请清理数据」 |
| 同步冲突 | 预览报告 + 操作表 | 「发现冲突，请选择安全同步或只同步无冲突项」 |
| 数据加载中 | 骨架屏 | — |

---

## 7. 功能映射

| 本页面功能 | 对应现有功能 | 迁移说明 |
|-----------|-------------|---------|
| 柯基安全横幅 | 无（新增） | 新增品牌元素+安全提示 |
| 深色模式 | Header 主题切换按钮 | 移至设置页 |
| 应用主题色 | 无（新增） | Phase 1 仅 UI |
| 跟随系统 | 现有自动检测逻辑 | 增加手动开关 |
| 本地数据大小 | 无（新增） | Phase 1 mock |
| 存储空间 | 无（新增） | Phase 1 mock |
| 本地备份 | Header 导出按钮 | 移至设置页，增加备份概念 |
| 导入备份 | Header 导入按钮 | 移至设置页 |
| 导入对话 | ChatGPT Vault 对话归档导入 | 移至设置页独立入口 |
| 安全提示栏 | 无（新增） | 强调本地安全理念 |

---

## 8. 验收标准

- [ ] 柯基安全横幅正确显示插画和对话气泡
- [ ] 深色模式开关可切换主题
- [ ] 主题色圆点可选择（Phase 1 仅 UI）
- [ ] 跟随系统开关可切换
- [ ] 本地数据大小卡片显示（Phase 1 mock）
- [ ] 存储空间卡片显示环形图（Phase 1 mock）
- [ ] 3个备份恢复卡片正确显示
- [ ] 图片下载记录正确显示，并支持一键清空历史
- [ ] 图片下载记录长文件名、时间和平台标签不挤压、不逐字换行
- [ ] 本地备份功能正常
- [ ] 导入备份/导入对话功能正常
- [ ] 安全提示栏正确显示
- [ ] 主题切换后所有页面样式正确更新
- [ ] 局域网同步前能展示新增、跳过、冲突和 PC 独有数量
- [ ] 回传和双向同步冲突时能选择安全同步或只同步无冲突项
- [ ] 同步报告能展示冲突副本数量和 PC 同步前备份路径


## 主题色实装说明（已落地）

设置-外观页取色器切换后，全站品牌主色实时跟随，链路为：
取色器点击 → `setAccent()` → 根节点 `data-accent` + `localStorage` → CSS 覆盖 `--pc-accent`/`--m-accent` →
派生令牌（`color-mix()`）驱动主按钮渐变与投影、激活/选中高亮、聚焦环、水波纹、进度环、欢迎页主装饰。

- 预设：粉 / 蓝 / 绿 / 紫 / 黄（保持 5 个，不做自定义取色、不新增预设）。
- 语义色（成功绿、警告黄、危险红、信息蓝、分类标签、功能快捷卡）保持独立，不随主题色变化。
- PC 与移动端采用同一套 `color-mix()` 派生策略，切换观感一致。
