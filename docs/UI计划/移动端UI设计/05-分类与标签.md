# 界面5：分类与标签

> 版本：1.1 | 最后更新：2026-05-10

---

## 1. 页面定位与目标

用于管理分类和标签，包括：
- 查看和切换分类管理/标签管理
- 新建分类
- 排序、批量编辑、合并分类、删除
- 拖拽调整分类顺序

**核心目标**：让用户高效组织提示词的分类体系。

---

## 2. 页面结构

```
┌──────────────────────────────────────────────┤
│  ← 返回      分类与标签                       │
├──────────────────────────────────────────────┤
│  ┌──────────────┐ ┌──────────────┐           │
│  │ 分类管理 ✓   │ │  标签管理    │           │
│  │ 浅粉底+深粉字│ │  灰色未选中  │           │
│  └──────────────┘ └──────────────┘           │
├──────────────────────────────────────────────┤
│  分类管理                      [+ 新建分类]  │
│                                  粉色胶囊     │
│  ┌──────────────────────────────────────────┐│
│  │ 🎨 插画风格     36 个提示词      ··· ⋮⋮  ││
│  ├──────────────────────────────────────────┤│
│  │ 📷 写实摄影     28 个提示词      ··· ⋮⋮  ││
│  ├──────────────────────────────────────────┤│
│  │ 🚀 科幻未来     24 个提示词      ··· ⋮⋮  ││
│  ├──────────────────────────────────────────┤│
│  │ 🏯 国风古韵     18 个提示词      ··· ⋮⋮  ││
│  ├──────────────────────────────────────────┤│
│  │ 🏠 场景设计     32 个提示词      ··· ⋮⋮  ││
│  ├──────────────────────────────────────────┤│
│  │ 👤 人物角色     26 个提示词      ··· ⋮⋮  ││
│  └──────────────────────────────────────────┘│
│                                              │
│  快速操作                                     │
│  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐│
│  │  📊    │ │  ✏️    │ │  🔀    │ │  🗑️    ││
│  │  排序  │ │批量编辑│ │合并分类│ │  删除  ││
│  │  绿色  │ │  蓝色  │ │  紫色  │ │  粉色  ││
│  └────────┘ └────────┘ └────────┘ └────────┘│
│                                              │
│  ┌──────────────────────────────────────────┐│
│  │ 🐰  长按并拖拽可调整顺序哦~      ❤️      ││
│  │      浅黄色提示横幅                       ││
│  └──────────────────────────────────────────┘│
└──────────────────────────────────────────────┘
```

---

## 3. 视觉规范

### 3.1 页面专用颜色

| 元素 | 颜色 | 说明 |
|------|------|------|
| 分段控制器-选中 | `--m-pink-light` 背景 + `--m-pink` 文字 | 浅粉底+深粉字 |
| 分段控制器-未选中 | 透明背景 + `--m-text2` 文字 | 灰色 |
| 新建分类按钮 | `--m-pink` 背景 + 白色字 | 粉色胶囊 |
| 分类列表卡片 | `--m-surface` 背景 | 白色 |
| 分类图标底色 | 各分类对应色 | 彩色圆角方形 |
| 排序按钮 | `--m-green` 背景 + 白色字 | 绿色 |
| 批量编辑按钮 | `--m-blue` 背景 + 白色字 | 蓝色 |
| 合并分类按钮 | `--m-purple` 背景 + 白色字 | 紫色 |
| 删除按钮 | `--m-pink` 背景 + 白色字 | 粉色 |
| 底部提示横幅 | `--m-yellow-light` 背景 | 浅黄色 |

### 3.2 分类图标颜色映射

| 分类 | 图标底色 | 图标颜色 |
|------|---------|---------|
| 插画风格 | `--m-blue-light` | `--m-blue` |
| 写实摄影 | `--m-yellow-light` | `#C4A030` |
| 科幻未来 | `--m-purple-light` | `#8B6FCC` |
| 国风古韵 | `--m-pink-light` | `#D4567F` |
| 场景设计 | `--m-green-light` | `#3D9942` |
| 人物角色 | `#FFE0CC` | `#E07020` |

---

## 4. 组件清单

### 4.1 可复用组件

| 组件 | 用途 |
|------|------|
| `tag-pill` | 标签管理中的标签项 |

### 4.2 页面专用组件

| 组件 | 说明 |
|------|------|
| 分段控制器 | 分类管理/标签管理切换 |
| 分类列表项 | 图标+名称+数量+三点菜单+拖拽手柄 |
| 快速操作按钮组 | 4个彩色操作按钮 |
| 兔子提示横幅 | 底部浅黄色横幅+兔子插画 |

---

## 5. 数据绑定

| 数据 | 来源 | API/方法 |
|------|------|---------|
| 分类列表 | 文件夹查询 | `getFolders()` |
| 各分类下提示词数 | 聚合查询 | 按 folder_id 分组计数 |
| 标签列表 | 标签查询 | Phase 1 mock |

---

## 6. 交互规范

### 6.1 交互状态矩阵

| 元素 | 默认态 | 触摸态 | 选中态 |
|------|--------|--------|--------|
| 分段控制器-选中 | 浅粉底+深粉字 | — | 圆角背景 |
| 分段控制器-未选中 | 灰色文字 | 文字→`--m-text` | — |
| 分类列表项 | 白色背景 | 背景→`--m-surface2` | — |
| 三点菜单 | 灰色图标 | 图标→`--m-text` | 弹出菜单 |
| 拖拽手柄 | 灰色点阵 | 图标→`--m-text2` | 可拖拽 |
| 快速操作按钮 | 各自彩色 | `scale(0.95)` | — |
| 兔子横幅 | 浅黄背景 | — | — |

### 6.2 手势操作

| 手势 | 目标 | 行为 |
|------|------|------|
| 点击 | 分段控制器 | 切换分类/标签管理视图 |
| 点击 | 新建分类 | 弹出新建分类弹窗（名称+颜色） |
| 点击 | 分类列表项 | 展开该分类下的提示词列表 |
| 点击 | 三点菜单 | 弹出操作菜单（重命名/改颜色/删除） |
| 长按+拖拽 | 分类列表项 | 调整分类顺序 |
| 点击 | 排序按钮 | 进入排序模式 |
| 点击 | 批量编辑 | 进入多选编辑模式 |
| 点击 | 合并分类 | 弹出合并选择弹窗 |
| 点击 | 删除按钮 | 弹出删除确认弹窗 |
| 点击 | 返回按钮 | 返回上一页 |

### 6.3 动画过渡

| 动画 | 时长 | 说明 |
|------|------|------|
| 分段切换 | 0.25s | 内容区淡入切换 |
| 列表项拖拽 | 实时 | 跟随手指移动，其他项自动让位 |
| 列表项删除 | 0.25s | 向左滑出+高度收缩 |
| 快速操作按钮 | 0.15s | `scale(0.95)` 触摸反馈 |

### 6.4 异常状态

| 状态 | 视觉 | 文案 |
|------|------|------|
| 无分类 | 空状态插画 | 「还没有分类，点击上方新建吧~」 |
| 删除确认 | 弹出确认弹窗 | 「确定删除该分类？分类下的提示词将变为未分类」 |
| 合并冲突 | 弹出选择弹窗 | 「选择要合并的目标分类」 |

---

## 7. 功能映射

| 本页面功能 | 对应现有功能 | 迁移说明 |
|-----------|-------------|---------|
| 分类管理 | 侧边栏文件夹列表 | 独立管理页面 |
| 标签管理 | 无（新增） | Phase 1 仅 UI |
| 新建分类 | 侧边栏新建文件夹弹窗 | 位置调整 |
| 重命名分类 | 右键菜单「重命名文件夹」 | 触发方式调整 |
| 更改分类颜色 | 右键菜单「更改颜色」 | 触发方式调整 |
| 删除分类 | 右键菜单「删除文件夹」 | 触发方式调整 |
| 拖拽排序 | 无（新增） | 新增功能 |
| 批量编辑 | 无（新增） | 新增功能 |
| 合并分类 | 无（新增） | 新增功能 |
| 兔子提示横幅 | 无（新增） | 新增品牌元素 |

---

## 8. 验收标准

- [ ] 分段控制器可切换分类/标签管理
- [ ] 分类列表正确显示所有文件夹及数量
- [ ] 新建分类弹窗可输入名称和选择颜色
- [ ] 三点菜单弹出操作菜单
- [ ] 重命名/改颜色/删除功能正常
- [ ] 拖拽排序可调整分类顺序
- [x] 快速操作4个按钮正确显示（排序高亮拖拽手柄、批量编辑多选改色、合并分类弹窗、清理空分类）
- [x] 快速操作4个按钮功能实装（2026-05-10）
- [ ] 兔子提示横幅正确显示
- [ ] 标签管理视图显示标签列表（Phase 1 mock）
- [ ] 空状态正确显示
