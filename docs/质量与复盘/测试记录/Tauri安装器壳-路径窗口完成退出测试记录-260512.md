# Tauri安装器壳-路径窗口完成退出测试记录

## 测试范围

- 安装位置默认路径调整到下载目录。
- 目录浏览、路径展开和路径校验基础链路。
- 无边框窗口拖动、最小化和关闭。
- 完成页点击“完成”后的关闭链路。
- 安装器壳 release 构建。

## 验证命令

```powershell
node --check installer-shell/src/main.js
node --check installer-shell/src/pages/complete.js
node --check installer-shell/src/pages/install-location.js
cargo check --manifest-path installer-shell/src-tauri/Cargo.toml
python scripts\build_installer_shell_package.py --skip-pc-build
```

## 验证结果

- JS 语法检查通过。
- Rust `cargo check` 通过。
- `python scripts\build_installer_shell_package.py --skip-pc-build` 通过。
- 发布产物已生成到 `releases/PromptImageManager-Shell-Setup-2.3.1.exe`。
- 发布产物大小：`34859008` 字节。
- 真实 release EXE 启动后，窗口标题为 `提示词管家安装向导`。
- 真实 release EXE 中，自定义标题栏拖动有效，窗口位置可变化。
- 真实 release EXE 中，最小化按钮有效。
- 真实 release EXE 中，关闭按钮有效，点击后安装器壳进程退出。

## 残余风险

- 当前自动化点击完整安装流程时，本机截图环境中安装器壳右页内容出现视口裁切，坐标点击未稳定命中完整流程按钮，因此本记录未把“真实安装完成页点击完成”标记为全自动闭环通过。
- 代码侧已补齐 `core:window:allow-close` 权限，并将完成页关闭统一走 `closeCurrentWindow()`；建议下一轮在人工可见完整窗口或更稳定的桌面自动化环境中补充一次从欢迎页到完成页的点击闭环截图。
